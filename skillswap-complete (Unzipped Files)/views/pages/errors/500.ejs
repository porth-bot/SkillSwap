<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Server Error - SkillSwap</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary-500: #6366f1;
      --primary-600: #4f46e5;
      --gray-50: #f9fafb;
      --gray-100: #f3f4f6;
      --gray-400: #9ca3af;
      --gray-500: #6b7280;
      --gray-600: #4b5563;
      --gray-900: #111827;
      --error-50: #fef2f2;
      --error-100: #fee2e2;
      --error-500: #ef4444;
      --error-600: #dc2626;
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background: linear-gradient(135deg, var(--error-50) 0%, var(--gray-50) 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }
    
    .error-container {
      text-align: center;
      max-width: 500px;
    }
    
    .error-illustration {
      margin-bottom: 2rem;
    }
    
    .error-icon {
      width: 160px;
      height: 160px;
      margin: 0 auto;
      position: relative;
    }
    
    .server-box {
      width: 120px;
      height: 80px;
      background: linear-gradient(135deg, var(--gray-400) 0%, var(--gray-500) 100%);
      border-radius: 8px;
      position: absolute;
      left: 50%;
      top: 50%;
      transform: translate(-50%, -50%);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      gap: 8px;
    }
    
    .server-light {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      background: var(--error-500);
      animation: blink 1s ease-in-out infinite;
    }
    
    .server-slots {
      display: flex;
      gap: 4px;
    }
    
    .server-slot {
      width: 60px;
      height: 8px;
      background: var(--gray-600);
      border-radius: 2px;
    }
    
    .smoke {
      position: absolute;
      width: 20px;
      height: 20px;
      background: var(--gray-400);
      border-radius: 50%;
      opacity: 0;
      animation: smoke 2s ease-out infinite;
    }
    
    .smoke:nth-child(1) { left: 30px; top: 20px; animation-delay: 0s; }
    .smoke:nth-child(2) { left: 50px; top: 10px; animation-delay: 0.3s; }
    .smoke:nth-child(3) { left: 70px; top: 25px; animation-delay: 0.6s; }
    .smoke:nth-child(4) { left: 90px; top: 15px; animation-delay: 0.9s; }
    .smoke:nth-child(5) { left: 110px; top: 20px; animation-delay: 1.2s; }
    
    @keyframes blink {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.3; }
    }
    
    @keyframes smoke {
      0% {
        opacity: 0;
        transform: translateY(0) scale(0.5);
      }
      50% {
        opacity: 0.6;
      }
      100% {
        opacity: 0;
        transform: translateY(-40px) scale(1.5);
      }
    }
    
    .error-code {
      font-size: 6rem;
      font-weight: 700;
      color: var(--error-500);
      line-height: 1;
      margin-bottom: 1rem;
      text-shadow: 2px 2px 0 var(--error-100);
    }
    
    .error-title {
      font-size: 1.5rem;
      font-weight: 600;
      color: var(--gray-900);
      margin-bottom: 0.75rem;
    }
    
    .error-message {
      color: var(--gray-600);
      line-height: 1.6;
      margin-bottom: 2rem;
    }
    
    .error-details {
      background: var(--error-50);
      border: 1px solid var(--error-100);
      border-radius: 8px;
      padding: 1rem;
      margin-bottom: 2rem;
      text-align: left;
    }
    
    .error-details-header {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-bottom: 0.5rem;
      font-weight: 500;
      color: var(--error-600);
    }
    
    .error-details code {
      font-family: 'Monaco', 'Consolas', monospace;
      font-size: 0.75rem;
      color: var(--gray-600);
      background: white;
      padding: 0.5rem;
      border-radius: 4px;
      display: block;
      overflow-x: auto;
      white-space: pre-wrap;
      word-break: break-all;
    }
    
    .error-actions {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }
    
    .btn {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      font-size: 1rem;
      font-weight: 500;
      text-decoration: none;
      cursor: pointer;
      border: none;
      transition: all 0.2s;
    }
    
    .btn-primary {
      background: var(--primary-500);
      color: white;
    }
    
    .btn-primary:hover {
      background: var(--primary-600);
      transform: translateY(-1px);
    }
    
    .btn-secondary {
      background: white;
      color: var(--gray-600);
      border: 1px solid var(--gray-100);
    }
    
    .btn-secondary:hover {
      background: var(--gray-50);
      border-color: var(--gray-400);
    }
    
    .support-info {
      margin-top: 2rem;
      padding-top: 2rem;
      border-top: 1px solid var(--gray-100);
    }
    
    .support-info p {
      font-size: 0.875rem;
      color: var(--gray-500);
    }
    
    .support-info a {
      color: var(--primary-500);
      text-decoration: none;
    }
    
    .support-info a:hover {
      text-decoration: underline;
    }
    
    .error-id {
      display: inline-block;
      margin-top: 0.5rem;
      font-family: 'Monaco', 'Consolas', monospace;
      font-size: 0.75rem;
      color: var(--gray-400);
      background: var(--gray-100);
      padding: 0.25rem 0.5rem;
      border-radius: 4px;
    }
  </style>
</head>
<body>
  <div class="error-container">
    <div class="error-illustration">
      <div class="error-icon">
        <div class="smoke"></div>
        <div class="smoke"></div>
        <div class="smoke"></div>
        <div class="smoke"></div>
        <div class="smoke"></div>
        <div class="server-box">
          <div class="server-light"></div>
          <div class="server-slots">
            <div class="server-slot"></div>
          </div>
          <div class="server-slots">
            <div class="server-slot"></div>
          </div>
        </div>
      </div>
    </div>
    
    <div class="error-code">500</div>
    <h1 class="error-title">Something broke</h1>
    <p class="error-message">
      This is on us, not you. Something went wrong on our server. 
      We've been notified and are looking into it.
    </p>
    
    <% if (typeof error !== 'undefined' && error && process.env.NODE_ENV === 'development') { %>
    <div class="error-details">
      <div class="error-details-header">
        <span>⚠️</span>
        <span>Error Details (Development Only)</span>
      </div>
      <code><%= error.stack || error.message || error %></code>
    </div>
    <% } %>
    
    <div class="error-actions">
      <button onclick="window.location.reload()" class="btn btn-primary">
        <span>🔄</span>
        Try Again
      </button>
      <a href="/" class="btn btn-secondary">
        <span>🏠</span>
        Go Home
      </a>
      <a href="/contact" class="btn btn-secondary">
        <span>📧</span>
        Contact Support
      </a>
    </div>
    
    <div class="support-info">
      <p>
        If this problem persists, please <a href="/contact">contact support</a> 
        with the error ID below.
      </p>
      <span class="error-id">
        Error ID: <%= typeof errorId !== 'undefined' ? errorId : 'ERR-' + Date.now().toString(36).toUpperCase() %>
      </span>
    </div>
  </div>
</body>
</html>
