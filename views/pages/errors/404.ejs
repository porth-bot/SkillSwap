<%- include('../../partials/header', { title: '404 - Page Not Found - SkillSwap' }) %>

<main class="error-page">
  <div class="error-container">
    <div class="error-illustration">
      <svg viewBox="0 0 400 300" fill="none" xmlns="http://www.w3.org/2000/svg">
        <!-- Background circle -->
        <circle cx="200" cy="150" r="120" fill="#EFF6FF"/>
        
        <!-- 404 Text -->
        <text x="200" y="170" font-family="system-ui, sans-serif" font-size="80" font-weight="bold" fill="#2563EB" text-anchor="middle">404</text>
        
        <!-- Magnifying glass -->
        <circle cx="320" cy="200" r="35" stroke="#94A3B8" stroke-width="8" fill="none"/>
        <line x1="345" y1="225" x2="375" y2="255" stroke="#94A3B8" stroke-width="8" stroke-linecap="round"/>
        
        <!-- Question mark in magnifying glass -->
        <text x="320" y="210" font-family="system-ui, sans-serif" font-size="36" fill="#94A3B8" text-anchor="middle">?</text>
        
        <!-- Decorative elements -->
        <circle cx="80" cy="80" r="8" fill="#BFDBFE"/>
        <circle cx="340" cy="60" r="6" fill="#BFDBFE"/>
        <circle cx="60" cy="200" r="10" fill="#DBEAFE"/>
        <rect x="100" y="240" width="15" height="15" rx="3" fill="#DBEAFE" transform="rotate(15 107.5 247.5)"/>
        <rect x="280" y="260" width="12" height="12" rx="2" fill="#BFDBFE" transform="rotate(-10 286 266)"/>
      </svg>
    </div>

    <div class="error-content">
      <h1>Hmm, that page doesn't exist</h1>
      <p class="error-message">Either you typed something wrong, or we messed up and broke a link. Either way, here's some places that definitely do work:</p>
      
      <div class="error-suggestions">
        <div class="suggestion-links">
          <a href="/" class="suggestion-link">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
              <polyline points="9 22 9 12 15 12 15 22"/>
            </svg>
            <span>Home</span>
          </a>
          <a href="/explore" class="suggestion-link">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="11" cy="11" r="8"/>
              <line x1="21" y1="21" x2="16.65" y2="16.65"/>
            </svg>
            <span>Find Tutors</span>
          </a>
          <a href="/dashboard" class="suggestion-link">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="3" y="3" width="7" height="7"/>
              <rect x="14" y="3" width="7" height="7"/>
              <rect x="14" y="14" width="7" height="7"/>
              <rect x="3" y="14" width="7" height="7"/>
            </svg>
            <span>Dashboard</span>
          </a>
          <a href="/help" class="suggestion-link">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"/>
              <path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/>
              <line x1="12" y1="17" x2="12.01" y2="17"/>
            </svg>
            <span>Help</span>
          </a>
        </div>
      </div>

      <div class="error-search">
        <p>Or just search:</p>
        <form action="/explore" method="GET" class="search-form">
          <input type="text" name="q" placeholder="What are you looking for?" autocomplete="off">
          <button type="submit" class="btn btn-primary">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="11" cy="11" r="8"/>
              <line x1="21" y1="21" x2="16.65" y2="16.65"/>
            </svg>
          </button>
        </form>
      </div>

      <div class="error-report">
        <p>Think this is broken? <a href="/contact">Tell us</a></p>
      </div>
    </div>
  </div>
</main>

<style>
.error-page {
  min-height: calc(100vh - 140px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 2rem;
  background: linear-gradient(135deg, var(--gray-50) 0%, var(--primary-light) 100%);
}

.error-container {
  max-width: 600px;
  text-align: center;
}

.error-illustration {
  margin-bottom: 2rem;
}

.error-illustration svg {
  width: 100%;
  max-width: 400px;
  height: auto;
}

.error-content h1 {
  font-size: 2.5rem;
  color: var(--gray-900);
  margin-bottom: 1rem;
}

.error-message {
  font-size: 1.125rem;
  color: var(--gray-600);
  line-height: 1.7;
  margin-bottom: 2rem;
}

.error-suggestions {
  margin-bottom: 2rem;
}

.error-suggestions h2 {
  font-size: 1rem;
  color: var(--gray-700);
  margin-bottom: 1rem;
  font-weight: 500;
}

.suggestion-links {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 1rem;
}

.suggestion-link {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 1.25rem;
  background: white;
  border: 1px solid var(--gray-200);
  border-radius: var(--radius-md);
  color: var(--gray-700);
  text-decoration: none;
  font-weight: 500;
  transition: all 0.2s;
}

.suggestion-link:hover {
  border-color: var(--primary);
  color: var(--primary);
  box-shadow: var(--shadow-sm);
}

.suggestion-link svg {
  color: var(--gray-400);
}

.suggestion-link:hover svg {
  color: var(--primary);
}

.error-search {
  margin-bottom: 2rem;
}

.error-search p {
  color: var(--gray-600);
  margin-bottom: 0.75rem;
  font-size: 0.9375rem;
}

.search-form {
  display: flex;
  gap: 0.5rem;
  max-width: 400px;
  margin: 0 auto;
}

.search-form input {
  flex: 1;
  padding: 0.75rem 1rem;
  border: 1px solid var(--gray-300);
  border-radius: var(--radius-md);
  font-size: 1rem;
}

.search-form input:focus {
  outline: none;
  border-color: var(--primary);
  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
}

.search-form .btn {
  padding: 0.75rem 1rem;
}

.error-report {
  color: var(--gray-500);
  font-size: 0.875rem;
}

.error-report a {
  color: var(--primary);
  text-decoration: none;
}

.error-report a:hover {
  text-decoration: underline;
}

@media (max-width: 640px) {
  .error-content h1 {
    font-size: 2rem;
  }
  
  .error-message {
    font-size: 1rem;
  }
  
  .suggestion-links {
    flex-direction: column;
    align-items: center;
  }
  
  .suggestion-link {
    width: 100%;
    max-width: 250px;
    justify-content: center;
  }
}
</style>

<%- include('../../partials/footer') %>
